@model EmployeeManagement.Models.Employee

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title></title>
</head>
<body>
    <h3>@ViewBag.PageTitle</h3>

    <div>
        Name: @Model.Name
    </div>
    <div>
        Email: @Model.Email
    </div>
    <div>
        Department: @Model.Department
    </div>
</body>
</html>

<!--
    ViewData["PageTitle"]
    var employee = ViewData["Employee"] as EmployeeManagement.Models.Employee;
    employee.Name

    ViewBag.PageTitle
    ViewBag.Employee.Name

    (at)model EmployeeManagement.Models.Employee on top of the .cshtml page
    ViewBag.PageTitle
    (at)Model.Name
-->
