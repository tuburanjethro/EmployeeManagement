@model HomeDetailsViewModel

@{
    //Layout = "~/Views/Shared/_Layout.cshtml"; No longer needed because of _ViewStart.cshtml
    ViewBag.Title = "Employee Details";
}

<h3>@Model.PageTitle</h3>

<div>
    Name: @Model.Employee.Name
</div>
<div>
    Email: @Model.Employee.Email
</div>
<div>
    Department: @Model.Employee.Department
</div>

@section Scripts {
    <script src="~/js/CustomScript.js"></script>
}
<!--
    ViewData["PageTitle"]
    var employee = ViewData["Employee"] as EmployeeManagement.Models.Employee;
    employee.Name

    ViewBag.PageTitle
    ViewBag.Employee.Name

    (at)model EmployeeManagement.Models.Employee on top of the .cshtml page
    ViewBag.PageTitle
    (at)Model.Name
-->
<!-- section
    - User defined section that can be accessed by the _Layout.cshtml  
-->